/** Store platforms for cross-platform IAP (Android + iOS). */
const STORE_PLATFORM = Object.freeze({
  GOOGLE_PLAY: "GOOGLE_PLAY",
  APP_STORE: "APP_STORE",
});

module.exports = {
  STORE_PLATFORM,
};
